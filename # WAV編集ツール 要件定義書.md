# WAV編集ツール 要件定義書

## 📌 概要
ゲーム開発用の音声アセット（SE・鳴き声など）を調整するための、
ブラウザ単体（HTML1枚）で動作するWAVファイル編集ツール。
サーバー通信なし、ローカル完結、インストール不要。

## 🎯 主な利用シーン
- ゲームに使用する音声ファイル（鳴き声・SEなど）の長さ調整
- 音量が小さい/大きいアセットの調整
- 複数の音声ファイルをまとめて一括処理

## 🖥️ 技術要件
- HTML5 + JavaScript（Vanilla推奨、フレームワーク不要）
- Web Audio API（`AudioContext`, `decodeAudioData`, `AudioBuffer`）
- Canvas API（波形描画）
- File API（ドラッグ&ドロップ、複数ファイル読み込み）
- 完全ローカル処理（外部通信・サーバーアップロードなし）
- 単一HTMLファイルで完結（もしくはJS/CSS分割でも可、配布は簡単な方で）

---

## 🔧 必須機能（優先度：高）

### 1. ファイル入出力
- [ ] ドラッグ&ドロップでのファイル読み込み
- [ ] 複数ファイル同時読み込み（サイドバー等でファイル一覧管理）
- [ ] ファイルごとに個別編集・切り替え可能なタブ/リスト構造
- [ ] 編集後ファイルのダウンロード（元ファイル名 + サフィックスで自動命名、例: `bark_edited.wav`）
- [ ] 複数ファイルをまとめてZIP等で一括ダウンロード（可能であれば）

### 2. 秒単位カット機能
- [ ] 波形表示（Canvas）
- [ ] マウスドラッグでの範囲選択
- [ ] **選択範囲を「秒（小数点対応）」で数値入力できるUI**
      例：開始 `1.250` 秒 〜 終了 `3.800` 秒
- [ ] 「選択範囲を残す」「選択範囲を削除する」の両方に対応
- [ ] ズーム機能（波形を拡大して細かい位置調整）
- [ ] 現在の選択範囲の長さをリアルタイム表示（秒数）

### 3. 音量調整機能
- [ ] 選択範囲 or 全体に対するゲイン（音量）調整（dB指定 or %指定）
- [ ] ノーマライズ機能（音量を最大化、クリッピング防止）
- [ ] 調整前後の波形比較表示（プレビューで確認できるように）
- [ ] 音量調整のプレビュー再生（保存前に確認できる）

### 4. プレビュー再生
- [ ] 選択範囲のみ再生
- [ ] 全体再生（再生ヘッドを波形上に表示）
- [ ] 再生/停止/ループのショートカット（Spaceキー等）

---

## ⭐ 追加機能（優先度：中〜高、できれば実装）

### 編集系
- [ ] フェードイン/フェードアウト（秒数指定）
- [ ] 無音除去（前後の無音を自動トリミング、閾値dB指定）
- [ ] 複数ファイルの結合（順番入れ替え可能）
- [ ] 逆再生（リバース）

### UI/UX系
- [ ] Undo/Redo（Ctrl+Z / Ctrl+Y）
- [ ] ダークモード
- [ ] キーボードショートカット一覧表示
- [ ] 編集履歴のログ表示（何を切ったか等）

### 出力設定
- [ ] 出力サンプルレート指定（ゲームエンジン側の要件に合わせる、例: 44100Hz / 48000Hz）
- [ ] 出力ビット深度指定（16bit / 24bit / 32bit float）
- [ ] モノラル/ステレオ変換オプション

---

## 💤 任意機能（優先度：低、余力があれば）

- [ ] 再生速度変更（ピッチ保持オプション付き）
- [ ] 簡易EQ/フィルター（ローパス・ハイパス）
- [ ] マーカー機能（複数の切り出しポイントを事前設定）
- [ ] プロジェクト保存/読み込み（JSON形式で編集状態を保存）
- [ ] レスポンシブ対応（スマホ・タブレットでも操作可能に）

---

## 🖼️ UI構成イメージ（案）

```
┌─────────────────────────────────────┐
│  ファイルリスト（複数ファイル管理）        │
│  ├─ bark.wav        [編集中]         │
│  ├─ jump.wav                       │
│  └─ explosion.wav                  │
├─────────────────────────────────────┤
│                                     │
│      波形表示エリア（Canvas）           │
│      ドラッグで範囲選択可能             │
│                                     │
├─────────────────────────────────────┤
│ 開始: [1.250]秒  終了: [3.800]秒       │
│ 音量: [====●====] +3.0dB            │
├─────────────────────────────────────┤
│ [▶再生] [■停止] [カット] [削除] [保存] │
└─────────────────────────────────────┘
```

---

## 📁 出力仕様
- ファイル形式：WAV（PCM、ロスレス）
- ファイル命名規則：`{元ファイル名}_edited.wav`（重複時は連番付与）
- 複数ファイル処理時は個別 or 一括ダウンロード両対応が理想

---

## ✅ 開発時の優先順位（実装順の目安）

1. ファイル読み込み（複数対応） + 波形表示
2. 秒単位での範囲選択・数値入力UI
3. カット（トリミング）+ プレビュー再生
4. 音量調整（dB指定 + ノーマライズ）
5. 保存/ダウンロード（命名規則含む）
6. フェード・無音除去・結合などの追加機能
7. Undo/Redo、UI仕上げ

---

## 🎮 補足（ゲームアセット調整という用途を踏まえて）
- 音量調整は「dB単位」での指定が扱いやすい（ゲームエンジン側の音量パラメータとの整合性）
- 出力サンプルレート/ビット深度は、使用エンジン（Unity/Unreal等）の推奨設定に合わせられるようにしておくと後工程がスムーズ
- 複数の似た音声ファイル（鳴き声のバリエーション等）を一括で同じ音量に揃えたい場合、「一括ノーマライズ」的な機能があると実用的