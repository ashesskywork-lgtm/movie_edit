# MP4動画編集ツール

## 概要
ゲーム開発用の動画アセットを調整するための、ブラウザ単体（HTML1枚）で動作するMP4動画編集ツール。
`音声ファイルエディタ`（WAV編集ツール）からの派生プロジェクト。サーバー通信なし・ローカル完結・インストール不要で、
ダブルクリックで即座に使えることを重視する方針は継承する。詳細な機能スコープはspec-forgeで策定中（TBD）。
派生元の要件定義書は `# WAV編集ツール 要件定義書.md`（音声ツール時代の資料、参考情報として残置）。

## 技術スタック
- **言語**: HTML5 + Vanilla JavaScript（ES2020+、フレームワーク不使用）
- **動画処理**: WebCodecs API（`VideoDecoder`/`VideoEncoder`/`AudioDecoder`/`AudioEncoder`、ブラウザネイティブ、追加ライブラリ不要）
- **MP4コンテナ処理**: 外部ライブラリを埋め込み方式で使用（2026-08-30決定、[[docs/decisions.md]]参照）
  - 書き出し（mux）: `mp4-muxer` を想定
  - 読み込み（demux/解析）: `mp4box.js` を想定
  - いずれもソースコードをindex.html内に直接埋め込み、CDN読み込み・外部API呼び出しは行わない
- **描画**: Canvas API（波形・サムネイル等の描画）
- **ファイル入出力**: File API（ドラッグ&ドロップ、複数ファイル読み込み）
- **ビルドツール**: なし（トランスパイル不要、ブラウザネイティブ機能のみ）
- **対応環境**: 最新のChrome / Edgeのみ想定（Safari/Firefox非対応。WebCodecs APIの対応状況からも実質必須の制約）

## ディレクトリ構成
```
動画ファイルエディタ/
├── index.html                          # アプリ本体（HTML+CSS+JSをすべて内包、単一ファイル完結。MP4ライブラリのソースも埋め込む）
├── # WAV編集ツール 要件定義書.md          # 派生元（音声ツール）の要件定義書。参考資料として残置
├── docs/
│   ├── decisions.md                    # 設計・仕様の決定記録
│   ├── diary.md                        # 開発日記（前払い設計）
│   └── tasks.md                        # タスク管理
├── .gitignore
└── .claude/
    └── settings.json                   # Claudeの機密ファイル参照防止
```
`index.html` は派生元（音声編集ツール）のコードがそのまま残っている状態。動画編集機能への作り替えはこれから着手する。

## コマンド
```bash
# 開発（サーバー不要、ファイルをブラウザで直接開く）
start index.html   # Windows / PowerShellの場合

# ビルド
不要（単一HTML完結のため）
# Linter / フォーマッター
未設定（TBD、必要になれば導入を検討）
# テスト
未設定（TBD、手動でのブラウザ動作確認が中心）
# デプロイ
なし（ローカルファイルとして配布、要件によりサーバーアップロード不可）
```

## コーディング規約
- 変数・関数名は `camelCase`、定数は `UPPER_SNAKE_CASE`
- 非同期処理は必ず `async/await`（Promiseチェーン禁止）
- `AudioContext` はアプリ全体で1つだけ生成し使い回す（複数生成しない）
- 秒数は小数点3桁までを扱う基本単位とする（要件定義書のUI例に合わせる）
- ファイルごとの編集状態（波形データ・選択範囲・音量設定等）はオブジェクトにまとめて管理し、グローバル変数を乱立させない
- `<script type="module">` は使わない（`file://` で開く運用のためCORSエラーの原因になる）

## アーキテクチャ・設計方針
単一HTML内を以下の関心事で区切って実装する：
- **State管理**: ファイルごとの編集状態（元AudioBuffer・現在のAudioBuffer・選択範囲・音量設定）を保持するオブジェクト
- **ファイルI/O**: ドラッグ&ドロップ / 複数ファイル読み込み / ダウンロード（命名規則: `{元ファイル名}_edited.wav`）
- **波形描画**: Canvas上にAudioBufferの波形を描画し、ドラッグ選択・ズームを扱う
- **音声処理**: カット・音量調整・ノーマライズ・フェード等はすべて新しい`AudioBuffer`を生成して適用し、元データは破棄しない（Undo/Redo実装を見据えた設計）
- **UIイベント**: 数値入力（秒）とCanvas上のドラッグ選択を双方向に同期させる

## ドメイン知識・設計の背景（WHY）
- 単一HTML完結にしているのは、社内で動画アセット担当者にファイル1つを渡すだけで使える運用を想定しているため（サーバー環境・インストール作業を不要にする要件、派生元の音声ツールから継承）
- MP4コンテナ処理に限り外部ライブラリの埋め込みを許可しているのは、MP4のbox構造（moov/trak/stbl等）を自作すると実装コスト・不具合リスクが高く、実績あるライブラリを使う方が合理的と判断したため（2026-08-30決定、詳細はdecisions.md）
- 外部API・外部通信は方針として明確に禁止（ライブラリ埋め込みとは別軸の判断。話がややこしくなるものは持ち込まない、というユーザー方針）

## DO / DON'T
**DO**
- 編集操作は元データ（`AudioBuffer`/デコード済みフレーム等）を直接書き換えず、新規生成したデータを返す関数として実装する
- UIの数値入力とCanvas選択範囲は常に同期させる
- MP4コンテナ処理用ライブラリ（mp4-muxer/mp4box.js等）はソースコードをindex.html内に埋め込む形で使用する

**DON'T**
- 外部CDN・外部API・外部通信を追加しない（ライブラリの「埋め込み」は許可対象だが、ネットワーク越しの呼び出しは方針で明確に禁止）
- React等のフレームワークを導入しない
- サーバーサイド処理（アップロード等）を追加しない
- MP4コンテナ処理以外の用途で新たにライブラリを追加する場合は、名前・理由・既存依存への影響をセットで説明し許可を得ること（依存ゼロが原則で、MP4コンテナ処理は明示的な例外）
- 既存ファイルで対応できるなら新規ファイルを作らない

## AIへの指示
- 回答は日本語で行うこと
- コード変更時は変更箇所と理由を必ず説明すること
- 不明点は実装前に確認すること
- MP4コンテナ処理以外でライブラリ追加する場合は、名前・理由・既存依存への影響をセットで説明し許可を得ること

<!-- ▼ SKILLの【運用ルール（CLAUDE.md転記用）】を転記 ▼ -->

### セッション開始時（指示を待たず能動的に実行）
1. `docs/tasks.md` … 未完了・対応中タスク
2. `docs/diary.md` … 最新エントリ＝前回作業・中断箇所
3. `docs/decisions.md` … 設計方針・却下済み選択肢
4. 以下形式で報告：
```
📋 前回の状況を確認しました。
【対応中タスク】- {タスク名}（🔴/🟡/🟢）
【前回の作業】- {diary最新概要} / 中断箇所：{あれば}
【設計方針】- {decisions直近}
続きから始めますか？
```

### diary.md（作業ログ｜前払い設計）
セッション開始時に読む。書くのは：
- **作業開始時（前払い・エラー落ち対策）**：`## {YYYY-MM-DD}` → `### 🔄 作業開始`（目的／方針）
- **作業中（随時）**：30分超・返答待ち・エラー発生時は解決前に `### 📝 途中経過`（実施内容／現在地・次にやること）
- **完了/区切り時**：`### ✅ 完了` or `### ❌ 未解決`（結果／動作確認／次回）。エラー時は `### うまくいかなかったこと`（事象／試したこと／結果）
- 「今日はここまで」等 → 途中経過も含め全記録してから終了

### tasks.md（ステータス管理に専念｜詳細はdiaryが持つ）
セッション開始時・「進捗教えて」で読む。書くのは：新依頼受領時／課題発見時／進捗変化時／着手時「対応中」へ移動／完了時「完了」へ移動。
優先度：ユーザー明示があればそれを優先。なければClaudeが判断し確認（🔴他タスクをブロック・本番影響／🟡今週のマイルストーン／🟢それ以外）。

### decisions.md（なぜそうしたか｜設計ブレ防止）
セッション開始時に読む。設計・技術選定の**直前**に自問：「なぜ選ぶか説明できるか／却下した選択肢はあるか」→ YESなら実装前に記録してから進む。方針変更時・非自明な判断時も記録。

## 注意事項 ⚠️
- 要件定義書のファイル名は `# WAV編集ツール 要件定義書.md` で、先頭に `# ` を含む特殊なファイル名になっている。シェル操作時は必ずパス全体をクォートすること（派生元の資料であり、動画ツール用の要件定義はspec-forgeで別途策定中）
- `file://` で直接開く運用が前提のため、モジュール分割・fetchによる別ファイル読み込みは避け、単一HTMLに閉じること（埋め込むMP4ライブラリのソースもindex.html内に直接記述する）
- Safari非対応前提のため、Web Audio API/WebCodecs APIのベンダープレフィックスは考慮不要
- 動画は音声よりデータ量・メモリ消費が桁違いに大きいため、大きめのファイルでの動作検証を早めに行うこと
